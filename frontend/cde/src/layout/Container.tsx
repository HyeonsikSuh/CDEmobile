import styled from "styled-components";
import "./index.css"

export const Container = styled.div`
  max-width: calc(var(--wb-width) + var(--wb-padding) * 2);
  padding: 0 var(--wb-padding);
  margin: 0 auto;
`;